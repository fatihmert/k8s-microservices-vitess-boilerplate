# ==============================================================================
# 1. Web Uygulaması Dağıtımı (Custom Docker Image + Nginx + PHP-FPM)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: php-app
  template:
    metadata:
      labels:
        app: php-app
    spec:
      terminationGracePeriodSeconds: 30
      volumes:
        - name: app-files
          emptyDir: {}
        - name: nginx-config-volume
          configMap:
            name: nginx-config
      # Özel imajımızdaki uygulama dosyalarını ortak ortama kopyalayan Başlatma Konteyneri (initContainer)
      initContainers:
        - name: copy-app-files
          image: my-php-app:v1
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
          command: ["sh", "-c", "cp -r /var/www/html/* /app/"]
          volumeMounts:
            - name: app-files
              mountPath: /app
      containers:
        - name: nginx-container
          image: nginx:alpine
          securityContext:
            allowPrivilegeEscalation: false
          ports:
            - name: http
              containerPort: 80
            - name: nginx-status
              containerPort: 8081
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: app-files
              mountPath: /var/www/html
            - name: nginx-config-volume
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
        - name: php-container
          image: my-php-app:v1
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
          ports:
            - name: php-fpm
              containerPort: 9000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 9000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: app-files
              mountPath: /var/www/html
        - name: nginx-exporter
          image: nginx/nginx-prometheus-exporter:1.5.1
          securityContext:
            allowPrivilegeEscalation: false
          args:
            - --nginx.scrape-uri=http://127.0.0.1:8081/nginx_status
          ports:
            - name: nginx-metrics
              containerPort: 9113
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
---
# ==============================================================================
# 2. Web Uygulaması Dış Erişim Servisi (ClusterIP Service)
# ==============================================================================
apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app.kubernetes.io/name: cyclechain-app
spec:
  type: ClusterIP
  selector:
    app: php-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: nginx-metrics
      protocol: TCP
      port: 9113
      targetPort: 9113

